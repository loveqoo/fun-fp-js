# 적대적 리뷰 2차 — 앞 작업(260812-d36358) 회차 2 수정 대상 (28분, 백그라운드)

리뷰어: `staticland-reviewer` (도구 호출 55회)

> **파일명 주의**: 완료 조건에 `260812-d36358-2-*.md` 라 적었는데 하네스가 현재 작업 해시
> (`260812-fa055f`)를 강제한다. 리뷰 대상은 앞 작업이지만 **기록의 소유자는 이 작업**이다.
> 완료 조건을 쓸 때 파일명 규칙을 안 본 것이 실수다.

## 리뷰어가 먼저 짚은 것 — **검토 중 대상이 네 번 바뀌었다**

> "이 리뷰의 전제가 깨졌다. 검토 중 `index.js` 가 네 번 바뀌었다"
> (`b9d6cc6` → `74014af` → `d5dbad2` → `4773041`)

백그라운드 리뷰를 돌리면서 **그 사이에 회차 3·4를 진행했다.** 리뷰어는 브리핑을 증거로
쓰지 않고 최종 상태로 판정했다 — 옳은 처리다.

**규칙 21(백그라운드로 돌려라)의 부작용이다.** 대화는 안 막히지만 **대상이 움직인다.**

## 판정: 위반 7건

---

## 자체 감사와의 대조 — 이 리뷰의 가장 큰 값어치

결과가 오기 전에 회차 1 리뷰 12건을 **내가 먼저 감사**했다
(`.dev/log/260812-fa055f-1-self-audit.md`: 해결 11 / 부분 1).

### ① 내가 ✅ 로 본 것 중 리뷰어가 미해결로 본 것 — **2건**

| # | 내 판정 | 리뷰어 판정 |
| --- | --- | --- |
| **2** `identity` 의 Functor/Apply 층 | ✅ 해결 | ⚠ **`Applicative.Const` 에서 재발** — `Functor.of('const(array)')` THROW |
| **5** 에러 귀속 | ✅ 해결 | ⚠ **`runOptic` 의 `name` 이 뮤테이션 생존** — 잠복이 한 칸 옆으로 옮겨감 |

**둘 다 "고친 그 자리" 만 보고 "같은 유형이 다른 곳에 있나" 를 안 봤다.**
`identity` 를 3단으로 고치면서 **같은 회차에 새로 쓴 `Const`** 는 옛 모양 그대로 뒀다.
`preview`/`toList` 에 자기 검사를 붙이면서 **`runOptic` 의 에러 가지에 도달하는 경로가
`over`/`foldMapOf` 만 남았다**는 것을 안 봤다.

### ② 리뷰어가 잡았는데 내 후보 목록에 **없던 것** — 3건

| # | 내가 못 본 것 |
| --- | --- |
| **1** | `foldMapOf` 만 키를 못 받는다 — **자기가 부르는 `Applicative.Const` 는 받는데** |
| **4** | **내가 만든 「이름 규칙」 표가 8개를 오분류** |
| **7** | `index.js` 주석 3곳이 `toListOf`/`composeOptic` 을 부른다 |

**#4 가 가장 아프다.** 회차 1 리뷰 #11("근거를 지어냈다")의 처방으로 만든 표인데
그 표의 근거가 또 틀렸다:

```
표: "대문자 + .of(key) | 타입 클래스 — 레지스트리를 가진다 | 32"
실측: .of 로 거른 32개 중 레지스트리(.types) 보유는 24개
      나머지 8개 = Maybe, Either, Task, Free, Validation, Reader, Writer, State
      Maybe.types => undefined,  Maybe.of(1) => Just(1)  ← Applicative 의 of
```

**숫자 네 개는 맞았다. 틀린 것은 그 숫자에 붙인 의미다.** 그리고 "추측하지 말고
돌리십시오" 라고 써둔 스니펫이 틀린 목록을 만든다.

#7 도 같은 종류다 — 회차 1 리뷰의 "산문에 옛 이름 0건" 은 **`docs/` 만 본 결과**였고,
나는 그것을 "전부 확인됨" 으로 읽었다.

### ③ 내 후보에 있는데 리뷰어가 **안 잡은 것** — 1건

`normalizeTypeClassKey` 직접 테스트 없음. 리뷰어는 간접 테스트로 충분하다고 본 듯하다.
**내가 과민했다.**

### ④ 내가 ✅ 로 봤고 리뷰어도 동의한 것 — 8건

#3 · #4(d.ts 12/12) · #6 · #7(격자) · #8 · #9 · #11(관례 명문화 자체) · #12.
그리고 리뷰어가 회차 1의 「문제없음」 항목을 재확인해 **깨진 것 0건**.

---

## 처리

| # | 내용 | 상태 |
| --- | --- | --- |
| **4** | 「이름 규칙」 표 오분류 | ✅ `.types` 기준(24/15/2/40), `.of` 가 두 뜻임을 명시, **스니펫을 먼저 돌려 표와 대조** |
| **1** | `foldMapOf` 만 키 거부 | ✅ `foldMapOf('number', ...)` → 6 |
| **2** | `Const` 의 Functor/Apply 익명 | ✅ 3단 등록 |
| **3** | `Applicative.Const` 타입 선언 없음 | ✅ `ConstTypeLambda` + 오버로드. 뮤테이션 2건 검거 |
| **5** | `runOptic` 의 `name` 생존 | ✅ `over`/`foldMapOf` 라벨 단언. **뮤테이션 이번엔 검거** |
| **7** | `index.js` 주석 3곳 | ✅ |
| 6 | `docs/` 에 `plus(`·`identity`·`Const` 0건 — **다섯 표면** | ❌ 계획서가 범위 밖 명시 |

## 리뷰어가 브리핑 질문에 답한 것

- **`Symbols.Monoid` 요구가 이점 ③ 을 죽이는가 — 아니다.** 등록 안 한 `new Monoid(...)` 가
  다섯 경로 전부 통과. **죽이는 것은 "키 거부" 쪽이었다** (#1)
- **에러 귀속 8/8 확인** — 누락 없음
- **`baseline` 이 새 표면을 못 덮는 것은 도구의 한계** — HEAD 와 대조하므로 **HEAD 에 없는
  새 API 는 원리상 못 덮는다.** 새 표면은 테스트 + 뮤테이션이 맡아야 하고 실제로 그쪽은
  잘 돌고 있다

## 이 대조가 알려준 것

**내 감사는 "고친 자리" 를 보고 리뷰어는 "같은 유형이 어디 또 있나" 를 본다.**

②의 3건 중 2건(#1·#7)이 **"같은 것이 다른 곳에도 있다"** 였고, ①의 2건도 그랬다.
5건 중 4건이 같은 형태다 — **내 감사 범위가 좁은 게 아니라 감사 방향이 다르다.**
