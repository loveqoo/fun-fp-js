# .dev/log — 조사와 검증 기록

**개별 문서를 읽기 전에 여기서 무엇이 있는지 보라.** 실행 결과와 그 해석이 들어 있고,
계획서의 주장이 실제로 맞았는지가 여기서 갈린다.

## 두 종류가 섞여 있다

| 접미사 | 무엇 |
| --- | --- |
| `-verification` | 계획서의 완료 조건을 하나씩 대조한 결과. **틀린 조건도 여기서 드러난다** |
| 그 외 | 결정을 내리기 전의 조사 — 소스·git 이력·실측 |

## 조사 기록

| 파일 | 무엇을 밝혔나 |
| --- | --- |
| `260811-15c84c-2-view-semantics.md` | `view` 의 계약이 이미 세 곳(`Lens.d.ts`·`docs`·`CLAUDE.md`)에 적혀 있었다 — 옛 동작이 명세 위반이었다 |
| `260811-15c84c-3-plus-to-monoid.md` | `Plus` 가 `alt`+`zero` 를 다 가져 구조적으로 Monoid 다. `Monoid` 생성자가 `Symbols.Semigroup` 을 요구하는 것이 유일한 걸림돌 |
| `260811-15c84c-4-derivation-facts.md` | `register()` 가 `constructor.name` 을 키로 써서 유도 시 `Monoid.types['Monoid']` 가 생긴다 |
| `260812-d36358-1-module-shape.md` | optics 이름 11개의 파급이 호출 200여 곳이 아니라 **구조분해 39줄**이었다 — 여섯 회차 미룬 근거가 틀렸다 |
| `260812-fa055f-1-self-audit.md` | 리뷰어를 기다리며 12건을 자체 감사. **리뷰어와 대조한 결과가 이 기록의 값어치** |
| `260811-7652c9-1-review-probe.md` | 리뷰어에 위반 5건을 심어 검출력 확인 — 5건 전부 + 미기획 4건 검출 |

## 검증 기록에서 드러난 것

**계획서의 완료 조건 자체가 틀린 경우가 두 번 있었다.**

| | 조건 | 무엇이 틀렸나 |
| --- | --- | --- |
| `260811-15c84c-1` | "내부 교체이므로 동작이 같아야 한다" | `view` 3건이 달라졌다. 테스트가 없어 `npm test` 는 초록 |
| `260811-15c84c-2` | "원시값·객체 모두 동작하는지" | **둘 다 동종 입력** — 이종에서 던지는 회귀를 구조적으로 못 봤다 |

→ 검증 조건은 **"무엇이 바뀌었나" 가 아니라 "바뀐 것이 무엇을 건드리나"** 로 써라.
[`learning/INDEX.md`](../learning/INDEX.md) 규칙 12.
